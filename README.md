This project is a Python-based WhatsApp message automation tool built using the Twilio WhatsApp API.
It allows users to schedule WhatsApp messages to be sent at a specific date and time through a simple command-line interface (CLI).
